env:
  global:
    - LD_LIBRARY_PATH: /usr/local/lib
install:
    - curl https://static.rust-lang.org/rustup.sh | sudo bash
script:
    - cargo build
    - cargo test
    - cargo doc
os:
    - linux

